services:
  - type: web
    name: leaflock-store
    runtime: static
    plan: free
    buildCommand: echo "LeafLock Store static deploy"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: "20"