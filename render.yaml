services:
  - type: web
    name: leaflock-store
    runtime: static
    plan: free
    buildCommand: echo "LeafLock Store static deploy"
    staticPublishPath: .
    envVars:
      - key: NODE_VERSION
        value: "20"